fastapi
uvicorn
# Use binary wheels for pandas/numpy to avoid compilation errors
pandas>=1.3.0,<2.0.0
numpy<2.0.0
setuptools
wheel
# Install openoa without dependencies first to avoid conflicts, then dependencies
openoa @ git+https://github.com/NatLabRockies/OpenOA.git
streamlit
requests